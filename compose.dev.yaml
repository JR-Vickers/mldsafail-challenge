services:
  proxy:
    environment:
      MLDSAFAIL_DOMAIN: http://localhost
  web:
    environment:
      MLDSAFAIL_ENV: local
      MLDSAFAIL_SECRET_KEY: local-compose-development-only
  coordinator:
    volumes:
      - ./deploy/dev-hidden-seeds.json:/srv/mldsafail-evaluator/secrets/hidden-seeds.json:ro
