# Deployment secrets

Create `hidden-seeds.json` here on the evaluator host. It must use the same
bounded profile-to-seed-list shape as the public suite. This directory ignores
all secret files. Never commit, package, log, or copy the hidden suite into a
participant workspace.
